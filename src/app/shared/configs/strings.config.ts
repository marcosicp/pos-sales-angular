const strings = {
  badLogin: 'Usuario y/o constraseña inválidos',
  noInternet: 'Hubo un problema de conexion. Pruebe de conectarse en unos minutos',
  regex: {
    email: /^[a-zA-Z0-9.!#$%&’*+/=?^_`{|}~-]+@[a-zA-Z0-9-]+(?:\.[a-zA-Z0-9-]+)*$/
  }
};

export default strings;
